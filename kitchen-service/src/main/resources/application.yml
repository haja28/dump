spring:
  application:
    name: kitchen-service
  datasource:
    url: jdbc:mysql://localhost:3306/makanforyou
    username: root
    password: Rsaucm123$
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8082

logging:
  level:
    com.makanforyou: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
