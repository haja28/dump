spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/makanforyou
    username: root
    password: Rsaucm123$
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8084

logging:
  level:
    com.makanforyou: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# Cart configuration
cart:
  expiration:
    hours: 24          # Cart expires after 24 hours of inactivity
    warning-hours: 2   # Show warning when cart is about to expire (2 hours before)
    cron: "0 0 * * * *" # Run cleanup every hour at :00

